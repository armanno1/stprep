<nav class="navbar navbar-expand-lg" id='main-nav-bar'>
  <a class="navbar-brand" href="#">STPrep</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item active">
        <%= link_to "Home", root_path, class: "nav-link" %>
      </li>
      <% if user_signed_in? %>
        <li class="nav-item">
        <%= link_to "Logout", destroy_user_session_path, method: :delete, class: "nav-link" %>
        </li>
      <% end %>
      <li class="nav-item">
        <%= link_to "Courses", courses_path, class: "nav-link" %>
      </li>
      <% if user_signed_in? && current_user.admin? %>
        <li class="nav-item">
        <%= link_to "Create Course", new_course_path, class: "nav-link" %>
        </li>
        <li class="nav-item">
        <%= link_to "Create Station", new_station_path, class: "nav-link" %>
        </li>
      <% end %>
      <% if user_signed_in? && current_user.courses.any? %>
        <li class="nav-item">
        <%= link_to "My courses", my_courses_path, class: "nav-link" %>
        </li>
      <% end %>
    </ul>
  </div>
</nav>
