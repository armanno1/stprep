<nav class="navbar navbar-expand-lg navbar-dark fixed-top" id='main-nav-bar'>
  <%= link_to "stprep", root_path, class: 'navbar-brand' %>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <div class="navbar-nav mr-auto"></div>
    <ul class="navbar-nav">
      <% if user_signed_in? %>
        <li class="nav-item mx-2">
          <%= link_to "Logout", destroy_user_session_path, method: :delete, class: "nav-link" %>
        </li>
      <% else %>
        <li class="nav-item mx-2">
          <%= link_to "Login", new_user_session_path, class: "nav-link" %>
        </li>
      <% end %>
      <li class="nav-item mx-2">
        <%= link_to "Courses", courses_path, class: "nav-link" %>
      </li>
      <% if user_signed_in? && current_user.admin? %>
        <li class="nav-item mx-2">
          <%= link_to "Create Course", new_course_path, class: "nav-link" %>
        </li>
      <% end %>
      <% if user_signed_in? && current_user.courses.any? %>
        <li class="nav-item mx-2">
          <%= link_to_if(current_user.admin?, "All courses", my_courses_path, class: "nav-link"){} %>
          <%= link_to_if(!current_user.admin?, "My courses", my_courses_path, class: "nav-link"){} %>
        </li>
      <% end %>
    </ul>
  </div>
</nav>
