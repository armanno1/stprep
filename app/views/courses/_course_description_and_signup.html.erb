<div class="row">
  <div class="col-sm-12">
    <ul class="list-group">
      <li class="list-group-item"><%= @course.description %></li>
      <li class="list-group-item"><strong>Price:</strong> <%= "£" + "#{@course.price}" %></li>
    </ul>
  </div>
</div>
<div class='row mt-3'>
  <div class='col-sm-12'>
    <% if user_signed_in? %>
      <%= link_to "Sign up for this course", payments_path(course: @course), :method => :post, class: "btn btn-lg btn-primary sign-up-button", remote: true %>
    <% else %>
      <%= link_to t(".sign_up"), new_user_registration_path %>
    <% end %>
  </div>
</div>
<script src="https://js.stripe.com/v3/"></script>
